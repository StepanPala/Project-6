[pytest]
markers =
    search: Tests for product search and product page verification.
    alt_text: Tests to verify the alt text of product images.
    download: Tests for downloading files (e.g. instructions) and verifying their storage.
